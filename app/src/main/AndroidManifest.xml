<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ardhacodes.subs1_jetpack">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Subs1_jetpack"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.appintro.AppIntroActivity"></activity>
        <activity android:name=".ui.main.MainActivity" />
        <activity
            android:name=".ui.detail.DetailMovieTvActivity"
            android:label="@string/title_activity_detail_movie_tv"
            android:theme="@style/Theme.Subs1_jetpack.NoActionBar" /> <!-- <activity android:name=".read.ReaderMoviesTvActivity" /> -->
        <!-- <activity android:name=""> -->
        <activity android:name=".ui.splashscreen.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>